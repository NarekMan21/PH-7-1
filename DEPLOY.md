# Инструкция по деплою на Vercel

## 📋 Подготовка

1. Убедитесь, что все файлы готовы:
   - ✅ `api/index.js` - Serverless функция для Vercel
   - ✅ `vercel.json` - Конфигурация маршрутов
   - ✅ `package.json` - Зависимости проекта
   - ✅ `index.html` и `charts.html` - Фронтенд
   - ✅ `.gitignore` - Игнорируемые файлы

## 🚀 Деплой через Vercel CLI

1. Установите Vercel CLI (если еще не установлен):
```bash
npm i -g vercel
```

2. Войдите в Vercel:
```bash
vercel login
```

3. Деплой проекта:
```bash
vercel
```

4. Следуйте инструкциям:
   - Выберите проект (или создайте новый)
   - Подтвердите настройки
   - Дождитесь завершения деплоя

5. После деплоя вы получите URL вида: `https://ваш-проект.vercel.app`

## 🌐 Деплой через GitHub

1. Создайте репозиторий на GitHub

2. Закоммитьте код:
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git push -u origin main
```

3. Перейдите на [vercel.com](https://vercel.com)

4. Нажмите "New Project"

5. Импортируйте ваш GitHub репозиторий

6. Vercel автоматически определит настройки из `vercel.json`

7. Нажмите "Deploy"

## ⚙️ Настройка ESP8266 для Vercel

После деплоя обновите прошивку ESP8266:

1. Откройте `esp8266_firmware.ino`

2. Найдите строку:
```cpp
#define BACKEND_URL     "http://192.168.0.31:3000/save"
```

3. Замените на URL вашего Vercel проекта:
```cpp
#define BACKEND_URL     "https://ваш-проект.vercel.app/save"
```

4. Загрузите обновленную прошивку на ESP8266

## ⚠️ Важные замечания

### Хранение данных на Vercel

- Данные хранятся в `/tmp` директории (временное хранилище)
- **Данные могут быть потеряны** при:
  - Перезапуске сервера
  - Обновлении деплоя
  - Неактивности более 10 минут (cold start)

### Настройка Supabase (Рекомендуется) ✅

**Выбран Supabase** как лучший вариант для этого проекта:
- ✅ Бесплатный тариф: 500MB базы данных
- ✅ PostgreSQL - надежная реляционная БД
- ✅ Данные никогда не теряются
- ✅ Простой REST API
- ✅ Возможность скачивания базы данных с веб-страницы

#### Шаг 1: Создание проекта Supabase

1. Перейдите на [supabase.com](https://supabase.com)
2. Зарегистрируйтесь или войдите
3. Нажмите "New Project"
4. Заполните данные:
   - **Name**: sensor-data (или любое другое)
   - **Database Password**: придумайте надежный пароль (сохраните его!)
   - **Region**: выберите ближайший регион
5. Нажмите "Create new project"
6. Дождитесь создания проекта (2-3 минуты)

#### Шаг 2: Создание таблицы

1. В панели Supabase перейдите в **SQL Editor**
2. Выполните следующий SQL запрос:

```sql
-- Создание таблицы для данных датчиков
CREATE TABLE IF NOT EXISTS sensor_data (
    id BIGSERIAL PRIMARY KEY,
    timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    date TEXT NOT NULL,
    ip TEXT NOT NULL,
    t REAL,
    h REAL,
    ec REAL,
    ph REAL,
    n REAL,
    p REAL,
    k REAL,
    v BOOLEAN DEFAULT false
);

-- Создание индекса для быстрого поиска по времени
CREATE INDEX IF NOT EXISTS idx_sensor_data_timestamp ON sensor_data(timestamp DESC);

-- Создание индекса для поиска по IP
CREATE INDEX IF NOT EXISTS idx_sensor_data_ip ON sensor_data(ip);
```

3. Нажмите "Run" для выполнения запроса

#### Шаг 3: Получение ключей API

1. Перейдите в **Settings > API**
2. Скопируйте:
   - **Project URL** (например: `https://xxxxx.supabase.co`)
   - **anon public** key (длинная строка)

#### Шаг 4: Настройка переменных окружения в Vercel

1. Перейдите в ваш проект на [vercel.com](https://vercel.com)
2. Откройте **Settings > Environment Variables**
3. Добавьте две переменные:

   **Name**: `SUPABASE_URL`  
   **Value**: `https://xxxxx.supabase.co` (ваш Project URL)

   **Name**: `SUPABASE_KEY`  
   **Value**: `ваш-anon-key` (ваш anon public key)

4. Выберите **Environment**: Production, Preview, Development (все три)
5. Нажмите "Save"
6. Передеплойте проект: `vercel --prod` или через GitHub

#### Шаг 5: Проверка работы

1. Откройте ваш проект на Vercel
2. Нажмите кнопку **💾 Скачать БД** на главной странице
3. Если база пуста, отправьте тестовые данные с ESP8266
4. Проверьте данные в Supabase: **Table Editor > sensor_data**

#### Альтернативные варианты (если не используете Supabase)

1. **MongoDB Atlas** (бесплатный тариф)
2. **PlanetScale** (бесплатный MySQL)
3. **Vercel KV** (Redis от Vercel)

### Ограничения Vercel Free Tier

- 100 GB bandwidth в месяц
- Serverless Functions: 100 GB-hours в месяц
- Максимальное время выполнения функции: 10 секунд (Hobby), 60 секунд (Pro)

## 🔧 Обновление деплоя

После изменений в коде:

```bash
vercel --prod
```

Или просто сделайте `git push` если настроен автоматический деплой из GitHub.

## 📝 Проверка работы

После деплоя проверьте:

1. Откройте `https://ваш-проект.vercel.app`
2. Проверьте API: `https://ваш-проект.vercel.app/stats`
3. Отправьте тестовые данные с ESP8266

## 🐛 Отладка

Логи доступны в панели Vercel:
- Перейдите в ваш проект на vercel.com
- Откройте вкладку "Functions"
- Просматривайте логи выполнения

