# Инструкция по деплою на Vercel

## 📋 Подготовка

1. Убедитесь, что все файлы готовы:
   - ✅ `api/index.js` - Serverless функция для Vercel
   - ✅ `vercel.json` - Конфигурация маршрутов
   - ✅ `package.json` - Зависимости проекта
   - ✅ `index.html` и `charts.html` - Фронтенд
   - ✅ `.gitignore` - Игнорируемые файлы

## 🚀 Деплой через Vercel CLI

1. Установите Vercel CLI (если еще не установлен):
```bash
npm i -g vercel
```

2. Войдите в Vercel:
```bash
vercel login
```

3. Деплой проекта:
```bash
vercel
```

4. Следуйте инструкциям:
   - Выберите проект (или создайте новый)
   - Подтвердите настройки
   - Дождитесь завершения деплоя

5. После деплоя вы получите URL вида: `https://ваш-проект.vercel.app`

## 🌐 Деплой через GitHub

1. Создайте репозиторий на GitHub

2. Закоммитьте код:
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git push -u origin main
```

3. Перейдите на [vercel.com](https://vercel.com)

4. Нажмите "New Project"

5. Импортируйте ваш GitHub репозиторий

6. Vercel автоматически определит настройки из `vercel.json`

7. Нажмите "Deploy"

## ⚙️ Настройка ESP8266 для Vercel

После деплоя обновите прошивку ESP8266:

1. Откройте `esp8266_firmware.ino`

2. Найдите строку:
```cpp
#define BACKEND_URL     "http://192.168.0.31:3000/save"
```

3. Замените на URL вашего Vercel проекта:
```cpp
#define BACKEND_URL     "https://ваш-проект.vercel.app/save"
```

4. Загрузите обновленную прошивку на ESP8266

## ⚠️ Важные замечания

### Хранение данных на Vercel

- Данные хранятся в `/tmp` директории (временное хранилище)
- **Данные могут быть потеряны** при:
  - Перезапуске сервера
  - Обновлении деплоя
  - Неактивности более 10 минут (cold start)

### Рекомендации для production

Для постоянного хранения данных рекомендуется:

1. **MongoDB Atlas** (бесплатный тариф)
2. **Supabase** (бесплатный PostgreSQL)
3. **PlanetScale** (бесплатный MySQL)
4. **Vercel KV** (Redis от Vercel)

### Ограничения Vercel Free Tier

- 100 GB bandwidth в месяц
- Serverless Functions: 100 GB-hours в месяц
- Максимальное время выполнения функции: 10 секунд (Hobby), 60 секунд (Pro)

## 🔧 Обновление деплоя

После изменений в коде:

```bash
vercel --prod
```

Или просто сделайте `git push` если настроен автоматический деплой из GitHub.

## 📝 Проверка работы

После деплоя проверьте:

1. Откройте `https://ваш-проект.vercel.app`
2. Проверьте API: `https://ваш-проект.vercel.app/stats`
3. Отправьте тестовые данные с ESP8266

## 🐛 Отладка

Логи доступны в панели Vercel:
- Перейдите в ваш проект на vercel.com
- Откройте вкладку "Functions"
- Просматривайте логи выполнения

