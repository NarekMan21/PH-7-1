# Настройка локального сервера с Supabase

## ✅ Что уже сделано:

1. ✅ Создан файл `.env` с вашими данными Supabase
2. ✅ Обновлен `backend.js` для работы с Supabase
3. ✅ Установлен `dotenv` для чтения переменных окружения
4. ✅ ESP8266 продолжит работать без изменений (отправляет на `http://192.168.0.31:3000/save`)

## 📋 Следующие шаги:

### 1. Создайте таблицу в Supabase

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите проект `sensor-data` (dxbvnmvgrxsgexwtrhoh)
3. Перейдите в **SQL Editor**
4. Скопируйте и выполните SQL из файла `supabase_setup.sql`:

```sql
-- Создание таблицы для данных датчиков
CREATE TABLE IF NOT EXISTS sensor_data (
    id BIGSERIAL PRIMARY KEY,
    timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    date TEXT NOT NULL,
    ip TEXT NOT NULL,
    t REAL,
    h REAL,
    ec REAL,
    ph REAL,
    n REAL,
    p REAL,
    k REAL,
    v BOOLEAN DEFAULT false
);

-- Создание индексов
CREATE INDEX IF NOT EXISTS idx_sensor_data_timestamp ON sensor_data(timestamp DESC);
CREATE INDEX IF NOT EXISTS idx_sensor_data_ip ON sensor_data(ip);

-- Включение Row Level Security
ALTER TABLE sensor_data ENABLE ROW LEVEL SECURITY;

-- Политики доступа
CREATE POLICY "Allow public read access" ON sensor_data
    FOR SELECT USING (true);

CREATE POLICY "Allow public insert" ON sensor_data
    FOR INSERT WITH CHECK (true);

CREATE POLICY "Allow public delete" ON sensor_data
    FOR DELETE USING (true);
```

5. Нажмите **Run** для выполнения

### 2. Запустите локальный сервер

```bash
npm start
```

Вы должны увидеть:
```
✅ Используется Supabase для хранения данных
✅ Supabase клиент инициализирован
🚀 Бэкенд запущен на http://0.0.0.0:3000
💾 Хранилище: Supabase (https://dxbvnmvgrxsgexwtrhoh.supabase.co)
```

### 3. Проверьте работу

1. ESP8266 автоматически начнет отправлять данные на `http://192.168.0.31:3000/save`
2. Данные будут сохраняться в Supabase
3. Откройте `http://192.168.0.31:3000/index.html` в браузере
4. Нажмите кнопку **💾 Скачать БД** для проверки

### 4. Проверка в Supabase

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Перейдите в **Table Editor**
3. Выберите таблицу `sensor_data`
4. Вы должны увидеть записи от ESP8266

## 🔧 Переменные окружения

Файл `.env` уже создан с вашими данными:
- `SUPABASE_URL=https://dxbvnmvgrxsgexwtrhoh.supabase.co`
- `SUPABASE_KEY=ваш-ключ`

## ⚠️ Важно

- **ESP8266 не нужно перепрошивать** - он продолжит работать как раньше
- Данные теперь сохраняются в Supabase вместо локального файла
- Локальный файл `sensor_data.json` больше не используется (но остается как fallback)
- Все данные доступны через веб-интерфейс и кнопку "Скачать БД"

## 🐛 Устранение проблем

### Если видите "⚠️ Используется файловое хранилище":
- Проверьте, что файл `.env` существует и содержит правильные значения
- Убедитесь, что переменные `SUPABASE_URL` и `SUPABASE_KEY` установлены

### Если данные не сохраняются:
- Проверьте, что таблица `sensor_data` создана в Supabase
- Проверьте политики RLS (Row Level Security) в Supabase
- Посмотрите логи сервера на наличие ошибок

### Если ESP8266 не может подключиться:
- Убедитесь, что сервер запущен: `npm start`
- Проверьте, что порт 3000 не заблокирован брандмауэром
- Проверьте IP адрес в настройках ESP8266: `192.168.0.31:3000`

