# 🚀 Деплой на Vercel - Пошаговая инструкция

## 📋 Подготовка

✅ Все файлы готовы:
- `api/index.js` - Serverless функция
- `vercel.json` - Конфигурация маршрутов
- `package.json` - Зависимости
- `lib/db.js` - Модуль для Supabase

## 🔧 Шаг 1: Установка Vercel CLI

```bash
npm i -g vercel
```

## 🔐 Шаг 2: Вход в Vercel

```bash
vercel login
```

Следуйте инструкциям в браузере для авторизации.

## 📦 Шаг 3: Первый деплой

```bash
vercel
```

Следуйте инструкциям:
- **Set up and deploy?** → `Y`
- **Which scope?** → Выберите ваш аккаунт
- **Link to existing project?** → `N` (создаем новый)
- **What's your project's name?** → `sensor-backend` (или любое другое)
- **In which directory is your code located?** → `./` (текущая директория)

Дождитесь завершения деплоя.

## ⚙️ Шаг 4: Настройка переменных окружения

### Через Vercel CLI:

```bash
vercel env add SUPABASE_URL
# Введите значение: https://dxbvnmvgrxsgexwtrhoh.supabase.co
# Выберите окружения: Production, Preview, Development (все три)

vercel env add SUPABASE_KEY
# Введите значение: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImR4YnZubXZncnhzZ2V4d3RyaG9oIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjI5NTI0ODIsImV4cCI6MjA3ODUyODQ4Mn0.MyjcP2BptexI_u3djyNUMdE9sxEDEbuZdPnmMIaFSXI
# Выберите окружения: Production, Preview, Development (все три)
```

### Или через веб-интерфейс:

1. Откройте [vercel.com/dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Settings > Environment Variables**
4. Добавьте переменные:

   **Name**: `SUPABASE_URL`  
   **Value**: `https://dxbvnmvgrxsgexwtrhoh.supabase.co`  
   **Environment**: Production, Preview, Development (все три)

   **Name**: `SUPABASE_KEY`  
   **Value**: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImR4YnZubXZncnhzZ2V4d3RyaG9oIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjI5NTI0ODIsImV4cCI6MjA3ODUyODQ4Mn0.MyjcP2BptexI_u3djyNUMdE9sxEDEbuZdPnmMIaFSXI`  
   **Environment**: Production, Preview, Development (все три)

5. Нажмите **Save**

## 🔄 Шаг 5: Передеплой с переменными окружения

```bash
vercel --prod
```

Или через веб-интерфейс:
- Перейдите в проект на Vercel
- Откройте вкладку **Deployments**
- Нажмите **Redeploy** на последнем деплое

## ✅ Шаг 6: Проверка работы

1. Откройте ваш проект на Vercel (URL будет показан после деплоя)
2. Проверьте главную страницу: `https://ваш-проект.vercel.app`
3. Проверьте API: `https://ваш-проект.vercel.app/stats`
4. Нажмите кнопку **💾 Скачать БД** на главной странице

## 🔗 Шаг 7: Обновление ESP8266 (опционально)

Если хотите, чтобы ESP8266 отправлял данные напрямую на Vercel:

1. Откройте `esp8266_firmware.ino`
2. Найдите строку:
   ```cpp
   #define BACKEND_URL     "http://192.168.0.31:3000/save"
   ```
3. Замените на URL вашего Vercel проекта:
   ```cpp
   #define BACKEND_URL     "https://ваш-проект.vercel.app/save"
   ```
4. Загрузите обновленную прошивку на ESP8266

**Примечание**: Если оставить локальный IP, ESP8266 будет отправлять данные на ваш локальный сервер, который затем сохранит их в Supabase.

## 📊 Мониторинг

- **Логи**: Vercel Dashboard > Ваш проект > Functions > Логи
- **Метрики**: Vercel Dashboard > Ваш проект > Analytics
- **Данные**: Supabase Dashboard > Table Editor > sensor_data

## 🐛 Устранение проблем

### Ошибка "Supabase клиент не инициализирован"
- Проверьте, что переменные окружения добавлены в Vercel
- Убедитесь, что выбран правильный Environment (Production/Preview/Development)
- Передеплойте проект после добавления переменных

### Данные не сохраняются
- Проверьте, что таблица `sensor_data` создана в Supabase
- Проверьте политики RLS в Supabase
- Посмотрите логи в Vercel Dashboard

### 404 ошибка на страницах
- Проверьте `vercel.json` - маршруты должны быть настроены правильно
- Убедитесь, что файлы `index.html` и `charts.html` находятся в корне проекта

## 🔄 Обновление проекта

После изменений в коде:

```bash
git add .
git commit -m "Обновление проекта"
git push
```

Если настроен автоматический деплой из GitHub, Vercel автоматически задеплоит изменения.

Или вручную:

```bash
vercel --prod
```

## 📝 Полезные команды

```bash
# Просмотр переменных окружения
vercel env ls

# Удаление переменной
vercel env rm SUPABASE_URL

# Просмотр деплоев
vercel ls

# Просмотр логов
vercel logs
```

